<p id="notice"><%= notice %></p>

<h1>Listado de Empleados</h1>

<div class="media">
  <div class="media-body">
    <table class="table table-hover">
    <thead>
        <tr>
          <th>Nombre</th>
          <th>Télefono</th>
          <th>Tipo de trabajo</th>
          <th colspan="3"></th>
        </tr>
      </thead>

      <tbody>
        <% @employees.each do |employee| %>
          <tr>
            <td><%= employee.name %></td>
            <td><%= employee.phone %></td>
            <td><%= employee.job_type %></td>
            <td><%= link_to 'Ver', employee, class: 'btn btn-mini btn-default' %></td>
            <td><%= link_to 'Editar', edit_employee_path(employee),class: 'btn btn-mini btn-success' %></td>
            <td><%= link_to 'Borrar', employee, method: :delete, class: 'btn btn-mini btn-danger', data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  </div>
</div>

<br>

<%= link_to 'Agregar',new_employee_path, class: 'btn btn-mini btn-default' %>
